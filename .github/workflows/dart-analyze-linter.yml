name: Lint Code Base

on:
  pull_request:
    branches:
      - '**'
jobs:
  run-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Analyze Dart
        uses: zgosalvez/github-actions-analyze-dart@v1.7.0